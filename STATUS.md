# Verdict — Status

NEXT: review + commit the landing-page/gridType/installer-proposal work on `feature/mock-mode-simulation` (nothing committed yet), then verify against real API keys and a live deploy  by Claude Code  (last updated 2026-07-23)

Context: three parallel workstreams landed this session, all verified offline. (1) `/` is now a full marketing landing (sticky nav, hero, embedded HomeShell app section, problem/solution, how-it-works, stat tiles, trust strip, CTA band, footer — Stitch-derived design, locked tokens) and the intake is guided: per-month/per-year bill toggle (maps to monthlyBillEur), optional annual-kWh field, and a new 3-card grid-connection selector. (2) `gridType` ("on_grid" | "off_grid" | "hybrid", optional/additive in lib/contracts.ts + data/schema.ts — frozen-contract sign-off by integration captain) flows intake → /quote → sizeQuote's new pure `applyGridTypePolicy` (off_grid: battery ≥ 1.5× daily + hybrid inverter; hybrid: baseline battery + hybrid-capable inverter) → lead POST → installer detail badge + composeFromMarket recompute; lead/quote routes now fully Zod-validated; `data/fixtures/german_market_catalog.json` refreshed with real 2026 hardware (Aiko Neostar 2S+, Jinko Tiger Neo, Meyer Burger Black 395, Fronius GEN24, SMA Smart Energy, Huawei SUN2000, BYD HVS/HVM, Sungrow SBR, sonnen) — Codex heads-up: catalog + additive grid_type tests touch your zone. (3) Installer lead detail is proposal-first: system summary card with grid-type badge, full BoM table with catalog-derived per-line € (new components/installer/BillOfMaterials.tsx), financial block (total/savings/payback/25-yr ROI/margin/win-rate), Review→Accept→Offer stepper, technical evidence collapsed below. Verified: tsc clean, lint 0 errors, 46/46 vitest, mock_flow e2e passing (desktop; webkit binary still not installed for iphone project), off_grid API smoke honest (18.5 kWh battery = 1.5 × 12.3 daily, mock badges).
